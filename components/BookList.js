import React from 'react';
import { View, Text } from 'react-native';
import styles from '../styles/styles';


export default MainScreen = ( { navigation } ) => {
  return(
    <View style={styles.container}>
      <Text>Welcome to the BookList</Text>
    </View>
  )
}